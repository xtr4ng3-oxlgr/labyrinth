# Scenarios

LABYRINTH scenarios are JSON files stored in `scenarios/<scenario-id>/scenario.json`.

Each scenario defines:

- objective,
- brief,
- artifacts,
- hints,
- solution,
- success criteria.

Artifacts are written as text files into generated lab folders.
